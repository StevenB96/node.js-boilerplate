import {
} from '../Actions/appActions';

const initialState = {
  loading: false,
  error: null,
};

const app = (state = initialState, action) => {
  switch (action.type) {
    default:
      return state;
  }
};

export default app;
