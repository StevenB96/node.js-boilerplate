import React from 'react';
import { render, screen } from '@testing-library/react';
import '@testing-library/jest-dom';
import Layout from '.';

test('layout renders children', () => {
  render(<Layout><div id="child">Child Component</div></Layout>);
  const childComponent = document.getElementById('child');
  expect(childComponent).toBeTruthy();
});

// E.g.
// Assertions:
// toBeInTheDocument:
// toHaveTextContent:
// toHaveAttribute:
// toHaveStyle:
// toHaveClass:
// toBeTruthy:

// E.g.
// Queries:
// queryByText:
// queryByRole:
// queryByTestId:
// queryByPlaceholderText:
// queryByLabelText:
// getElementById: