import {
} from '../actions/app_actions';

const initialState = {
  loading: false,
  error: null,
};

const app = (state = initialState, action) => {
  switch (action.type) {
    default:
      return state;
  }
};

export default app;
